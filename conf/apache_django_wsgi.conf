Alias /media/ "${buildout:directory}/static/media/"

<Directory "${buildout:directory}/static/media/">
  Order allow,deny
  Options None
  Allow from all
</Directory>

WSGIScriptAlias / "${buildout:directory}/mysite/apache/mlgb.wsgi"

<Directory "${buildout:directory}/mysite/apache">
  Order allow,deny
  Allow from all
</Directory>

AddType application/rdf+xml .rdf